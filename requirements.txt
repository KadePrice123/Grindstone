fastapi==0.141.1
uvicorn[standard]==0.52.1
argon2-cffi==25.1.0
cryptography==50.0.0
httpx==0.28.1
RapidFuzz==3.14.5
# yfinance + curl_cffi were REMOVED 2026-08-02. The keyless market fallback
# now calls Yahoo's chart endpoint over httpx directly: importing yfinance
# pulled in pandas, cost ~8s, and held the GIL long enough to freeze the
# whole sidecar (it hung sign-in once and tripped the data breaker once).
# One documented JSON endpoint answers in ~0.5s and carries both the quote
# and the OHLC series, so nothing was lost by dropping the pair.
ddgs==9.14.4
trafilatura>=2.2,<3.0
# Backtest engine (backend/bt/, vendored 2026-08-03). numpy is imported ONLY
# by the runner subprocess (backend/bt_runner.py) and the engine's own tests —
# never by the sidecar process (see backend/bt/__init__.py for why). pytest
# runs the engine's unit suite inside the verification gate.
numpy>=2.0,<3.0
pytest>=8.0,<9.0
# zoneinfo's tz database is absent on Windows; the recorded-data sync
# (backend/btdata.py) converts snapshot timestamps to US/Eastern trading days
# and falls back to fixed EST without this.
tzdata>=2024.1
